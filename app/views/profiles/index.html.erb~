<h1>Listing profiles</h1>

<table id="hor-minimalist-a" summary="Employee Pay Sheet" style="width:800px;">
  <thead>
    <tr>
      <th>Full name</th>
      <th>Camera type</th>
      <th>Camera model name</th>
      <th>Profession</th>
      <th>Mobile number</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @profiles.each do |profile| %>
      <tr>
        <td><b><%= link_to profile.full_name, profile %></b></td>
        <td><%= profile.camera_type.name %></td>
        <td><%= profile.camera_model_name %></td>
        <td><%= profile.profession %></td>
        <td><%= profile.mobile_number %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<b/><%= button_to 'New Profile', new_profile_path(user_id: current_user.id), method: :get %></b>
